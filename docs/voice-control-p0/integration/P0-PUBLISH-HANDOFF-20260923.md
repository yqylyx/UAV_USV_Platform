# P0 验收成果交接

发布分支：`mxy/p0-local-acceptance-20260922`，仓库 `yqylyx/UAV_USV_Platform`。本文件随验收修复提交；实际远端发布结果以分支提交记录为准。

本机隔离环境96/96通过，受测基础ae46345f及修复的源码指纹、原始日志、数据库关联证据均已归档。主报告：[P0最终验收](P0-FINAL-ACCEPTANCE-20260922.md)；逐项结果：[96条CSV](P0-CASE-COVERAGE-20260922.csv)。

## 本次内容

包括P07业务异常、P13正常退出清理竞态、R06旧策略校验、P14自然终态、P17容量边界、I05接口守卫、展示恢复修复及配套回归。P17此前由Python同学提交，本分支包含其已复测实现，合并时不要重复覆盖。

后端615次测试执行、MySQL32项、前端32项、Python20项通过；615包含继承与重复并发测试，不能当成615条独立验收用例。真实浏览器四动作、30秒STALE、重新同步恢复通过，数据库恰好4条执行，STOP退出码0。

## 给测试同学

1. 获取此分支，在自己的联调分支合并；不要重置或强推共享分支。优先合并完整分支以保留之前的集成提交。
2. 本分支未擅自合并远端后续改动；如最新基线修改同一文件，请按最终行为解决冲突并回归。不要只挑报告提交而遗漏源码修复。
3. 保留独立数据库、测试账号和本地凭据文件；仓库不包含本机密码、Cookie或安装依赖目录。
4. 按最终报告比对版本。原始证据保留失败尝试和修复后结果，历史快照中的未上传/未完成描述不是最新进度。
5. 合并后至少在目标环境复核真实四动作、STALE恢复、STOP退出及开关关闭兼容。96/96结论仅属于本机受测版本，不能自动套用于不同合并结果。

## 给前端与Python/Unity同学

前端核对I05守卫及展示恢复、缺失实例指引；Python核对P07/P14/P17；后端清理和策略检查由本次提交提供。以协议与日志关联ID核对，不覆盖既有Unity构建。

## 验收口径

R02按原接口契约区分尚待确认的GENERATION_MISMATCH与已失效提案的PROPOSAL_INVALIDATED，均409且不得向新运行发送命令。X13不得把重启后LOST旧运行伪装成场景就绪。F10没有专用TTL删除任务，验收的是实际清理入口与记录保留。

原始证据禁用Git换行转换；源码指纹为受测本机字节，跨平台换行可能改变字节哈希，应同时核对Git提交和实际代码。final-manifest中的uncommitted/uploaded是验收当时快照，不作为发布状态查询。

功能开关保持关闭，是否正式启用由团队在合入目标基线并完成环境复核后决定。
