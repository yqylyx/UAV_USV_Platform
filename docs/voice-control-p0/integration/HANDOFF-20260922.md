# 2026-09-22 测试基线增量合并与交接

## 基线与合并

- 以测试同学 origin/lyj/p0-test-20260921 的 225b12f 为起点。
- 已包含成员一前端最新提交，本轮未重复合并前端。
- 后端新增修复 b35310a；合并提交 2758d50。
- Python 最新 062aadd52e1f79a1210e5d6dff8d872971d282b5；合并提交 2e102ff。
- Unity 是独立仓库 Albatrossii/UAV_USV_Unity_VirtualFleet；最新已核对版本 890128f。不能将 Unity Git 提交直接合进 Platform 分支。本轮未覆盖 Unity 工作区，也未构建或部署 WebGL。

## 本轮实测

仅运行与新增后端修复和 Python 合并相关的增量测试：

| 项目 | 结果 | 数量 |
|---|---|---|
| 真实算法帧设备名单回归 | PASS | 3 |
| HTTP 鉴权与 CSRF 回归 | PASS | 6 |
| 错误集成令牌返回 401 而非 500 | PASS | 1 |
| 合并后真实 Java—Python 链路 | PASS | 1（覆盖四动作） |
| 总计 | PASS | 11，失败 0，错误 0，跳过 0 |

真实链路检查包括 prepare、Ready、心跳、暂停期间心跳、START/PAUSE/RESUME/STOP、六设备名单、commandId 与运行身份、状态版本、成功回执、STOP 后退出。采用真实 GB_SFLA_CS Python adapter 与 Java 进程管理器，数据库为独立内存 H2；并非 HTTP 或浏览器验收。

证据：[真实链路 JSON](evidence/java-python-062aadd-20260922.json)。其中 startedAt 是实际执行时间；上下文和 execution 的时间字段来自固定测试时钟，不应当作现场业务时间或数据库验收证据。

## 已完成工作不重复执行

以下采用测试同学此前报告，不冒充本轮重新执行：prepare 6/6 样例、48 个契约正负样例及计划哈希、Fake Runner、前端类型检查和构建、普通后端全量测试、本机 MySQL 32/32、Flyway V19，以及模拟/自动化展示状态流转。

本次不连接或更改业务数据库，不重新启用服务开关，不上传凭据。新增测试没有数据库结构修改，不需要新迁移。

## 待完成验收及阻断

| 场景 | 当前状态 | 后续责任 |
|---|---|---|
| Java—Python 真实四动作及 STOP 回执 | PASS | 本轮增量已完成 |
| Unity 消息桥 | BLOCKED | 成员一、三统一消息封装与网页转发入口 |
| Unity SCENE_READY 和真实 FRAME_APPLIED | BLOCKED | 消息桥修复后提供新 WebGL 并实测 |
| 浏览器提案确认到真实 REPORTED_APPLIED | BLOCKED | 新 WebGL 就绪后四人联合操作 |
| 30 秒 STALE、重新同步、刷新恢复、旧挑战/旧代次隔离现场验收 | BLOCKED | 真实展示链路通后记录浏览器、数据库及 Runner/Unity 证据 |
| 最终全链路权限、幂等、重复/并发确认 | 未做现场验收 | 既有自动化结果已保留，联合环境再验收 |

阻断依据：当前前端 SimulationUnityWebglPanel.postPresentationEnvelope 发送原始消息；Unity 890128f 的 VueWebGlVirtualFleet.index.html 仍要求 data.source='vue-console' 且存在 data.message，且未增加 PRESENTATION 分派。890128f 修复的是 SCENE_READY executionId 的 null 序列化，未解决该入口问题。不得用模拟报告代替真实帧应用成功。

## 测试同学接手

1. 拉取已更新的 lyj/p0-test-20260921；有本地未提交工作时先自行保存，勿强制重置。
2. 本轮 11 项结果和 JSON 可直接归档，无需为了重复记录再次跑全量测试。
3. 收到成员一、三消息桥修复后，记录 Platform、Unity、WebGL 构建版本，执行上表 BLOCKED 场景；逐项附 PASS/FAIL/BLOCKED 及证据。
4. 完成真实链路验收后，再由主仓库维护者合并最终集成分支。
