# mxy D1 后端交接与测试报告

分支：mxy/p1-backend-20260923，起点09863cd。当前交付Java实现、配置、工具和自动化证据；不声称D1浏览器验收完成。

## 已实现

POST /api/voice/intelligence/transcriptions；ADMIN/当前权限/CSRF、multipart内存限制和真实非阻塞上传期限；全局1000条内存结果、终态30分钟/每分钟清理，活跃不清理、容量满不阻断已有键；单任务/速率、原结果重放；本机ASR内部Bearer及期限适配，严格JSON/ID/revision校验及错误映射；意图入口保持关闭；脱敏完成日志。V20新增7天最小受理墓碑，不保存音频或转写文字，不修改算法Runner或Unity。

上传专门绕开Servlet multipart落盘与旧P0 JSON-only过滤，其他P0路径继续原保护。受理墓碑在调用Python前写入；写入失败直接503，不降级调用。Java重启后原键同内容返回409 VOICE_REQUEST_OUTCOME_UNKNOWN，改内容返回409 IDEMPOTENCY_CONFLICT，均不会再次推理。内存仍有结果时保持原响应重放。

## 验证

当前自动化67项全部通过：D1新增53项、既有P0 HTTP14项。范围包括JUnit+H2权限、真实Spring Security链、真实随机端口Tomcat登录/CSRF/multipart、真实本机HTTP适配模拟服务、并发/1000容量/30分钟时间边界、7天墓碑、Java实例重建防重复推理、真实HTTP 409映射及慢上传10秒拒绝。内嵌HTTP使用合成音频和测试provider，不能作为真实模型识别通过。

另有1项本机MySQL专项通过：在随机临时库完整执行Flyway V1–V20，验证V20表及NEW/MATCH受理行为，结束后自动删除临时库。首次读取本地YAML时因引号被误作密码而连接失败，未创建数据库；修正读取后复测通过。PlatformContextIntegrationTests使用默认H2配置时会在既有V1的MySQL专用UNIQUE KEY语法处失败，该失败早于V20；本轮以真实MySQL完整迁移结果作为V20证据，未将该H2配置问题记为V20通过。

契约Schema及54个正反样例通过。原v1.1 WAV正例因D1格式收窄明确改为拒绝用例；内部响应Schema及新增错误已同步。E1/意图结构保留但未启用。

第一次实际上传测试发现同步InputStream关闭导致拒绝延迟，已改为Servlet非阻塞上传并复测通过。证据见evidence/mxy-d1-backend-20260923/test-summary.json；逐条D1状态见D1-验收执行台账-20260923.csv。

## 配置和复测

application-d1-asr.yml与start-d1-java.ps1；默认检查，需打包后运行。启动实际服务需要nly内部ASR就绪，环境变量名和隔离说明见README.md。当前运行的P0前后端未替换，未启用尚未交付的D1真实服务。

verify-d1-http.py用于后续真实HTTP识别及同键重放，凭据只从环境读取，输出不含完整文字/音频/Cookie。脚本不自动重试新推理，不代替真实浏览器验收。

## nly交接阻塞与下一步

nly的f630a74已无冲突合入mxy联调分支。合并后前端86项及生产构建通过；Python增加Windows未读请求体响应稳定性和流式SHA256兼容修复后19项通过，非法超时头场景连续复测10次通过。mxy现有Python 3.9环境可运行，但与nly锁定的Python 3.13环境不同，不能将本机依赖环境标记为锁文件复现通过。

真实small模型四文件指纹匹配并成功预热。用户提供的微信MP3原文件严格解码返回415；仅在诊断工具中内存重编码后，MP3和WebM真实HTTP识别成功，静音返回422。随后经Java完成登录、CSRF、真实识别和同键重放：200 SUCCESS、durationMs=5784、耗时4.64秒、重放响应一致。Java重启后原键返回409 VOICE_REQUEST_OUTCOME_UNKNOWN，同键改内容返回409 IDEMPOTENCY_CONFLICT，Python审计调用计数始终为1。

D1尚未结束：真实模型HTTP和Java进程重启防重已经通过；localhost浏览器麦克风、58秒尾帧、拒绝权限、前端10分钟窗口、退出/换账号、不触发控制及P0/Unity并行仍待联合验证。E1原46条保持待测，未以自动化数字冒充完整验收。
