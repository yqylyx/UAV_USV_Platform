# mxy D1 后端交接与测试报告

分支：mxy/p1-backend-20260923，起点09863cd。当前交付Java实现、配置、工具和自动化证据；不声称D1浏览器验收完成。

## 已实现

POST /api/voice/intelligence/transcriptions；ADMIN/当前权限/CSRF、multipart内存限制和真实非阻塞上传期限；全局1000条幂等，终态30分钟/每分钟清理，活跃不清理、容量满不阻断已有键；单任务/速率、原结果重放；本机ASR内部Bearer及期限适配，严格JSON/ID/revision校验及错误映射；意图入口保持关闭；脱敏完成日志。无新增迁移，不修改算法Runner或Unity。

上传专门绕开Servlet multipart落盘与旧P0 JSON-only过滤，其他P0路径继续原保护。内部超时或不确定传输失败后Java保守暂停新请求，直到mxy确认旧ASR进程退出并协调重启；既有键可重放。此为D1人工恢复，不提供跨重启保证。

## 验证

当前自动化59项全部通过：D1新增45项、既有P0 HTTP14项。范围包括JUnit+H2权限、真实Spring Security链、真实随机端口Tomcat登录/CSRF/multipart、真实本机HTTP适配模拟服务、并发/1000容量/30分钟时间边界及慢上传10秒拒绝。内嵌HTTP使用合成音频和测试provider，不能作为真实模型识别通过。

契约Schema及54个正反样例通过。原v1.1 WAV正例因D1格式收窄明确改为拒绝用例；内部响应Schema及新增错误已同步。E1/意图结构保留但未启用。

第一次实际上传测试发现同步InputStream关闭导致拒绝延迟，已改为Servlet非阻塞上传并复测通过。证据见evidence/mxy-d1-backend-20260923/test-summary.json；逐条D1状态见D1-验收执行台账-20260923.csv。

## 配置和复测

application-d1-asr.yml与start-d1-java.ps1；默认检查，需打包后运行。启动实际服务需要nly内部ASR就绪，环境变量名和隔离说明见README.md。当前运行的P0前后端未替换，未启用尚未交付的D1真实服务。

verify-d1-http.py用于后续真实HTTP识别及同键重放，凭据只从环境读取，输出不含完整文字/音频/Cookie。脚本不自动重试新推理，不代替真实浏览器验收。

## nly交接阻塞与下一步

本次拉取远端仍为f603b01；未发现ASR服务代码，本机18082未监听。请nly提交独立ASR服务及ASR-only前端，提供commit、模型revision、实际启动/ready、内部密钥配置方式和Chrome/Edge版本。mxy随后同机部署，执行真实文件HTTP、localhost麦克风、取消/换账号、静音、尾帧、不触发控制及Unity兼容；nly复核关键演示。

D1尚未结束：前端10分钟窗口、实际codec/时长/静音、Python从请求头起busy、真实模型/浏览器/P0并行、人工重启全过程仍待验证。E1原46条保持待测，未以自动化数字冒充完整验收。
